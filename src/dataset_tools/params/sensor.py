
class Sensor:
    def __init__(self, name, type, sample_rate, resolution, range):
        self.name = name
        self.type = type
        self.sample_rate = sample_rate
        self.resolution = resolution
        self.range = range
